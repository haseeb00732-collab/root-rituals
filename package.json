{
  "name": "root-rituals",
  "version": "1.0.0",
  "private": true,
  "description": "Root Rituals — interactive dark-theme botanical apothecary site",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "start": "npx serve . -l ${PORT:-3000}"
  }
}
